{
  "name": "vision-mcp",
  "version": "1.1.0",
  "description": "MCP server that reads/analyzes images via a remote OpenAI-compatible vision model (mimo-v2.5 / \"vision\")",
  "type": "module",
  "bin": {
    "vision-mcp": "dist/index.js"
  },
  "main": "dist/index.js",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node test/client.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  },
  "license": "MIT"
}
